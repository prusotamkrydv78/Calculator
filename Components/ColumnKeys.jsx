import { View, Text, Pressable } from "react-native";
import React from "react";
import styles from "../Styles/MainScreen";

const ColumnKeys = ({ key1, key2, key3, key4, yellow, green }) => {
  return (
  

    </View>
  );
};

export default ColumnKeys;
